{% extends '::base.html.twig' %}

{% block body -%}
	<div class="row">
		<div class="col-md-10">
			<ul class="nav nav-pills">
				{% for batiment in batiments %}
				<li ><a href="#bat_{{batiment.batiment}}" >{{ batiment.batiment }} <span class="badge">{{ batiment.doors|length }}</span></a></li>
				{% endfor %}
			</ul>
		</div>
		<div class="col-md-2">
			<a class="btn btn-success" href="{{ path('door_new') }}">Ajouter une porte</a>
		</div>
	</div>
	
	{% for batiment in batiments %}
	<hr />
	<h2 id="bat_{{batiment.batiment}}">Bâtiment {{batiment.batiment}}</h2>
	<div class="row">
		{% for door in batiment.doors %}
		<div class="col-6 col-sm-6 col-lg-4" style="margin-bottom: 10px;">
			<h4>{{ door.location }}</h4>
			<p style="height: 30px;">{{ door.description }}</p>
		 
			<div>
				<a href="{{ path('door_show', { 'id': door.id }) }}" class="btn btn-primary"><i class="glyphicon glyphicon-eye-open"></i> Consulter</a>
				<a href="{{ path('door_edit', { 'id': door.id }) }}" class="btn btn-info"><i class="glyphicon glyphicon-edit"></i> Modifier</a>
			</div>
		</div>
		{% endfor %}
	</div>
    {% endfor %}
    {% endblock %}
